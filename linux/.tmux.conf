# No visual bell
set -g visual-bell off
set -g bell-action none

# Allow alternate screen (default in tmux)
set -g alternate-screen on

# Disable flow control
set -g flow-control off

# default key-bindings (prefix is C-^)
# C-^ d       => detach the session
# C-^ [0-9]   => switch to the nth window
# C-^ c       => create a new window
# C-^ w       => list windows (interactive chooser)
# C-^ &       => kill current window
# C-^ t       => show current time
# C-^ [       => enter copy-mode, scroll with PageUp/PageDown
# C-^ ]       => paste from buffer
# C-^ "       => split pane horizontally
# C-^ %       => split pane vertically
# C-^ o       => switch to next pane
# C-^ x       => kill current pane
# C-^ z       => zoom/unzoom current pane
# C-^ ,       => rename current window
# C-^ $       => rename current session
# tmux kill-session -t [session-name] => kill a tmux session
# tmux list-sessions => list tmux sessions

# Prefix key: C-^ (Control-Shift-6)
unbind C-b
set -g prefix C-^
bind C-^ send-prefix

# Scrollback buffer
set -g history-limit 8000

# Allow xterm scrollback
set -g terminal-overrides 'xterm*:smcup@:rmcup@'

# Status bar (bottom line, similar to hardstatus)
set -g status on
set -g status-style "bg=default"
set -g status-left ""
set -g status-right "#[fg=green]%I%p %a, %Y-%m-%d"
set -g window-status-format "#[fg=blue]#I*#W"
set -g window-status-current-format "#[fg=white,bg=blue,bold]#I*#W"

# Create named windows (indices 1-9)
set -g base-index 1
new-session -d -s main -n epg_1
new-window -t main:2 -n epg_2
new-window -t main:3 -n epg_3
new-window -t main:4 -n epg_4
new-window -t main:5 -n lab_0
new-window -t main:6 -n lab_1
new-window -t main:7 -n misc_0
new-window -t main:8 -n misc_1
new-window -t main:9 -n misc_2

# F1-F9 to select windows 1-9
bind -n F1 select-window -t 1
bind -n F2 select-window -t 2
bind -n F3 select-window -t 3
bind -n F4 select-window -t 4
bind -n F5 select-window -t 5
bind -n F6 select-window -t 6
bind -n F7 select-window -t 7
bind -n F8 select-window -t 8
bind -n F9 select-window -t 9
